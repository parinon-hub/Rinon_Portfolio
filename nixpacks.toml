[phases.build]
dependsOn = ["install"]
cmds = [
    "...",
    "npm install",
    "npm run build",
    "php artisan optimize"
]

[phases.start]
cmds = [
    "php artisan migrate --force --seed",
    "..."
]
